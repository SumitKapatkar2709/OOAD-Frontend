import { AppointmentStudent } from './appointment-student';

describe('AppointmentStudent', () => {
  it('should create an instance', () => {
    expect(new AppointmentStudent()).toBeTruthy();
  });
});
